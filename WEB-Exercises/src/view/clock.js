const SECONDE = 1000; // 1sec = 1000milliseconden

/**
 * This function should render the element identified by '.clock'
 * by setting the textContent to the current time with the formaat HH:MM:SS.
 */
function render() {
}

render();

